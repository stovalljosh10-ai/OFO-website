name: Deploy to Staging

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH to InstaWP Staging
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 64.227.108.149
          username: caruyuloru8018
          password: ${{ secrets.INSTAWP_SSH_PASSWORD }}
          port: 22
          script: |
            cd /var/www/html/wp-content/themes/bricks-child
            git clone https://github.com/stovalljosh10-ai/OFO-website.git temp-repo 2>/dev/null || (cd temp-repo && git pull)
            cp -rf temp-repo/. ./
            rm -rf temp-repo/.git
