# Order Fireworks Online — Website Project

orderfireworksonline.com
WordPress + WooCommerce + Bricks Builder

## Structure
- `theme/` — child theme files
- `bricks-templates/` — exported Bricks JSON
- `custom-css/` — standalone CSS overrides
- `custom-js/` — standalone JS files